/**
 * jQuery UI shim for Vite builds.
 *
 * jQuery UI is loaded via global jQuery before Vite bundle.
 * This shim is just a no-op to satisfy import statements.
 */

// jQuery UI is already loaded globally, nothing to export
export {};
